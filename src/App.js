// npm i axios

// Axios
// returns promise
// response data located in data object
// error in response.error

const App = () => {
  return (
    <div>
      <header>Learn React</header>
    </div>
  );
};

export default App;
